# 3D代码可视化功能

## 功能概述

成功为代码可视化分析器添加了3D可视化功能，使用Three.js库创建交互式的3D代码依赖图谱。

## 核心特性

### 🎯 3D可视化引擎
- **Three.js集成**: 使用WebGL进行高性能3D渲染
- **多种几何体**: 根据符号类型显示不同形状（球体、立方体、八面体等）
- **颜色编码**: 不同类型符号使用不同颜色区分
- **光照系统**: 环境光、方向光、点光源组合照明

### 🎮 交互控制
- **轨道控制器**: 鼠标拖拽旋转视角
- **缩放功能**: 滚轮缩放查看细节
- **平移功能**: 右键拖拽平移视图
- **重置视图**: 一键恢复默认视角

### ⚡ 性能优化
- **LOD系统**: 根据距离动态调整几何体细节
- **视锥体剔除**: 只渲染相机视野内的对象
- **节点数量限制**: 最大1000个节点，确保性能
- **智能动画**: 节点较少时才执行旋转动画
- **批量清理**: 优化内存管理和资源释放

### 📊 智能布局
- **模块分组**: 按模块分组显示节点
- **力导向布局**: 自动计算节点位置避免重叠
- **层次结构**: 支持父子符号关系展示

## 技术实现

### 文件结构
```
src/renderer/
├── 3d-visualizer.js      # 核心3D可视化逻辑
├── three-controls.js     # 自定义相机控制器
└── index.html           # 更新包含Three.js库
```

### 主要组件

#### 3D可视化器 (3d-visualizer.js)
- `initScene()`: 初始化3D场景、相机、渲染器
- `create3DNodes()`: 创建3D节点，支持LOD优化
- `create3DEdges()`: 创建表示依赖关系的3D线条
- `animate()`: 动画循环，包含性能优化
- `updateLOD()`: 动态更新细节层次
- `frustumCulling()`: 视锥体剔除算法

#### 相机控制器 (three-controls.js)
- 自定义OrbitControls实现
- 支持旋转、缩放、平移操作
- 阻尼效果使移动更平滑

### 性能优化策略

1. **LOD (Level of Detail)**
   - 近距离：32段细分的高质量几何体
   - 中等距离：16段细分的标准质量
   - 远距离：8段细分的低质量几何体

2. **视锥体剔除**
   - 只渲染相机视野内的对象
   - 减少GPU渲染负担
   - 显著提升大场景性能

3. **智能更新频率**
   - LOD更新每3帧执行一次
   - 平衡性能和视觉效果

4. **内存管理**
   - 批量清理几何体和材质
   - 及时释放WebGL资源
   - 避免内存泄漏

## 使用方法

### 启动3D可视化
1. 分析项目后，点击右下角的"3D"按钮
2. 等待3D场景初始化完成
3. 使用鼠标进行交互操作

### 交互操作
- **左键拖拽**: 旋转视角
- **滚轮**: 缩放视图
- **右键拖拽**: 平移视图
- **重置按钮**: 恢复默认视角

### 视觉元素
- **球体**: 函数和方法
- **立方体**: 类定义
- **八面体**: 接口
- **四面体**: 常量
- **锥体**: 其他符号

## 性能指标

- **节点数量**: 支持最多1000个节点
- **帧率**: 在标准硬件上保持60FPS
- **内存使用**: 优化的内存管理，避免泄漏
- **加载时间**: 快速初始化，秒级响应

## 未来改进方向

1. **更多交互功能**
   - 节点点击查看详细信息
   - 依赖关系高亮显示
   - 搜索和筛选功能

2. **视觉效果增强**
   - 粒子效果
   - 更复杂的光照模型
   - 后处理效果

3. **性能进一步优化**
   - 实例化渲染
   - Web Workers并行计算
   - GPU加速布局算法

4. **VR/AR支持**
   - WebXR API集成
   - 虚拟现实查看
   - 增强现实叠加

这个3D可视化功能大大增强了代码分析工具的用户体验，使复杂的代码依赖关系变得更加直观和易于理解。